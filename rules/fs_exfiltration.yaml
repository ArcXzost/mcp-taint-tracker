name: "Data Exfiltration"
severity: "CRITICAL"
pattern:
  source:
    taints: ["filesystem", "credential"]
  path:
    max_hops: 5
    requires_taint: true
  sink:
    tools: ["http_request", "http_post", "webhook"]
